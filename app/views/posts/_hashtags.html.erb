<% unless @post.hashtags.empty? %>
<table align="center">
	<tr>
	<% @post.hashtags.each do |tag| %>
		<td><%= link_to tag.tag, show_post_path(current_user, @post) %></td>
	<% end %>
	</tr>
</table>
<% end %>