<%= form_for @post, :html => {:multipart => true, "data-ajax" => false} do |f| %>
	<p>
		<%= f.label "What's new?" %>
		<%= f.text_area :text %>
	</p>
	<p>
		<%= f.file_field :image %>
	</p>
	<p>
		<%= f.submit "Post" %>
	</p>
<% end %>