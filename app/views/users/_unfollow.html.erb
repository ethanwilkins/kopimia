<%= link_to(unfollow_path(current_user.connections.find_by(followed_id: @user.id).id),
	method: :delete, class: "btn btn-default") do %>
	<span class="glyphicon glyphicon-minus-sign"></span>
<% end %>