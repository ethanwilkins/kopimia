<!-- users -->

<%= render "users/profile" %>

<%= render "posts/new" if current_user and current_user.id == @user.id %>

<%= render "wall" %>

<% if @user.posts.size > page_size and @posts.size >= page_size %>
	<%= render "pages/more" %>
<% end %>