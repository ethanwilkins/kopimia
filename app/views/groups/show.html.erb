<div id="card">
	<div class="row">
		<h1 class="left"><%= @group.name %></h1>
		<div class="right"><%= render "join" %></div>
	</div>
	<div class="column">
		<div align="center"><%= image_tag @group.icon, width: "90%" %></div>
		<p align="center"><%= @group.description %></p>
	</div>
</div>
<br>

<%= render "posts/new", group: @group.id %>

<%= render "feed" %>