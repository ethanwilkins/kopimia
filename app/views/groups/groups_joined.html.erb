<h1>Groups</h1>

<% @groups.each do |group| %>
<div id="card">
	<table>
		<tr>
			<td><%= link_to image_tag(group.icon, size: "50x50"), group_path(group) %></td>
			<td><%= link_to group.name.capitalize, group_path(group) %></td>
		</tr>
	</table>
</div>
<% end %>