<% unless page == :feed_page and Post.from_users_followed_by(current_user).size < page_size %>
	<div align="center" id="more_button">
		<div id="large_button">
			<%= link_to(more_path(page: page), class: "btn btn-default btn-lg btn-block") do %>
				More
			<% end %>
		</div>
	</div>
<% end %>