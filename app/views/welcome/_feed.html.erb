<!-- main feed -->

<%= render "posts/new" %>

<% @feed.each do |post| %>
	<%= render "posts/card", post: post %>
<% end %>

<%= render "pages/more_back", all_items: User.feed(current_user), items: @feed %>