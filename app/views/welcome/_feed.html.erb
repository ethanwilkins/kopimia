<!-- main feed -->

<%= render "posts/new" %>

<% @feed.each do |post| %>
	<%= render "posts/card", post: post %>
<% end %>

<% if Post.from_users_followed_by(current_user).empty? %>
	<%= render "search/search_bar" %>
<% end %>

<%= render("pages/older_newer", page: :feed_page) %>