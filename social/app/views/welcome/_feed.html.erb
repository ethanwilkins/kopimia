<h2>Feed:</h2>
<table>
	<% @feed.each do |post| %>
	<tr>
		<td><%= link_to gravatar_for(User.find(post.user_id)), user_path(post.user_id) %></td>
		<td><h3><%= link_to User.find(post.user_id).name + ":", user_path(post.user_id) %></h3></td>
		<td><%= post.text %></td>
	</tr>
	<% end %>
</table>