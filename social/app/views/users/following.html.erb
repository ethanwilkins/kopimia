<h1>Following</h1>

<table>
  <% @users.each do |user| %>
    <tr>
		<td><%= link_to profile_picture(user), user_path(user) %></td>
		<td><%= link_to user.name.capitalize, user_path(user) %></td>
    </tr>
  <% end %>
</table>